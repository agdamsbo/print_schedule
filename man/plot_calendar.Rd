% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_calendar.R
\name{plot_calendar}
\alias{plot_calendar}
\title{Monthly, yearly, and week-block calendars}
\usage{
plot_calendar(
  year = as.integer(format(Sys.Date(), "\%Y")),
  month = NULL,
  from = NULL,
  to = NULL,
  view = c("month", "week"),
  start = c("S", "M"),
  orientation = c("landscape", "portrait"),
  title,
  title.size = 20,
  title.col = "gray30",
  subtitle = "",
  subtitle.size = 10,
  subtitle.col = "gray30",
  text = NULL,
  text.pos = NULL,
  text.size = 8,
  text.col = "gray30",
  events = NULL,
  palette = NULL,
  special.days = NULL,
  special.col = "gray90",
  gradient = FALSE,
  low.col = "white",
  col = "gray30",
  lwd = 0.5,
  lty = 1,
  font.family = "sans",
  font.style = "plain",
  day.size = 3,
  days.col = "gray30",
  weeknames,
  weeknames.col = "gray30",
  weeknames.size = 4.5,
  week.number = FALSE,
  week.number.col = "gray30",
  week.number.size = 8,
  monthnames,
  months.size = 10,
  months.col = "gray30",
  months.pos = 0.5,
  mbg.col = "white",
  legend.pos = "bottom",
  legend.title = "",
  bg.col = "white",
  margin = 1,
  ncol
)
}
\arguments{
\item{year}{Calendar year (default: current year).}

\item{month}{Numeric month 1–12, or NULL for a full-year calendar.}

\item{from}{Custom start date (overrides year/month).}

\item{to}{Custom end date (required when from is set).}

\item{view}{\code{"month"} (default) or \code{"week"} for a rolling week-block view.}

\item{start}{\code{"S"} (Sunday, default) or \code{"M"} (Monday).}

\item{orientation}{\code{"landscape"} (default) or \code{"portrait"}.}

\item{title}{Main title. Auto-generated when missing.}

\item{title.size, title.col}{Title font size and colour.}

\item{subtitle, subtitle.size, subtitle.col}{Optional italic subtitle.}

\item{text, text.pos, text.size, text.col}{Character annotations on specific
day positions (monthly view only).}

\item{events}{A data.frame with columns \code{date} (Date or "YYYY-MM-DD"),
\code{label} (character), and optionally \code{category} (character).}

\item{palette}{Named character vector mapping category names to colours,
e.g. \code{c(Holiday = "#4A90D9", Meeting = "#E86C3A")}.}

\item{special.days}{Integer vector of day indices to highlight, or
\code{"weekend"}. Ignored when \code{events} is supplied.}

\item{special.col}{Highlight colour (or upper gradient colour).}

\item{gradient}{Logical. Treat \code{special.days} as a continuous gradient.}

\item{low.col}{Lower gradient / background tile colour (default \code{"white"}).}

\item{col, lwd, lty}{Grid line colour, width, linetype.}

\item{font.family, font.style}{Font family and style for all text.}

\item{day.size, days.col}{Day-number font size and colour.}

\item{weeknames, weeknames.col, weeknames.size}{Day-of-week header labels,
colour, and size.}

\item{week.number, week.number.col, week.number.size}{Show ISO week numbers
on the y-axis.}

\item{monthnames, months.size, months.col, months.pos, mbg.col}{Month label
customisation (yearly view).}

\item{legend.pos, legend.title}{Legend position and title.}

\item{bg.col}{Background colour.}

\item{margin}{Numeric margin multiplier.}

\item{ncol}{Number of facet columns (yearly / week-block view).}
}
\value{
A \pkg{ggplot2} object.
}
\description{
Monthly, yearly, and week-block calendars
}
\examples{
# Month view
plot_calendar(year = 2024, month = 6)

# Week-block view
plot_calendar(from = "2024-06-01", to = "2024-08-31", view = "week")

# With events
events <- data.frame(
  date = as.Date(c("2024-06-15", "2024-06-20")),
  label = c("Meeting", "Holiday"),
  category = c("Work", "Personal")
)
plot_calendar(year = 2024, month = 6, events = events)

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny_utils.R
\name{colorSelectInput}
\alias{colorSelectInput}
\title{Color Select Input}
\usage{
colorSelectInput(
  inputId,
  label,
  choices,
  selected = NULL,
  previews = 4,
  ...,
  placeholder = ""
)
}
\arguments{
\item{inputId}{The input slot that will be used to access the value.}

\item{label}{Display label for the control, or NULL for no label.}

\item{choices}{List of values to select from. Can be a named vector
where names are display labels.}

\item{selected}{The initially selected value (if not specified then
defaults to the first value).}

\item{previews}{Number of color swatches to display (default: 4).}

\item{...}{Additional arguments passed to \code{\link[shiny:selectInput]{shiny::selectizeInput()}}.}

\item{placeholder}{A string to show as placeholder (not currently used).}
}
\value{
A selectize input control with custom rendering for color previews.
}
\description{
A custom Shiny select input that displays color palette previews as
swatches alongside palette names. Useful for letting users choose
color schemes in a visual way.
}
\details{
The function generates color previews using \code{\link[=generate_colors]{generate_colors()}} and
displays them as small colored boxes next to palette names in the
dropdown. When a palette is selected, the swatches are shown inline
with the selected value.
}
\examples{
\dontrun{
colorSelectInput(
  inputId = "palette",
  label = "Choose a color palette:",
  choices = c("Viridis" = "viridis", "Spectral" = "Spectral")
)
}

}
